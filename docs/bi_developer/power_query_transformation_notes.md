# Power Query Transformation Notes


Power Query should promote headers, set explicit data types, preserve key names, avoid unnecessary transformations, and leave governed KPI calculations to DAX measures.

Use Folder import for `data/exports/powerbi_handoff/`, then rename tables cleanly. Do not create new business logic in Power Query without updating KPI docs.


These BI documents complement the completed dashboard file committed at `dashboards/powerbi/p2_marketing_performance_dashboard.pbix`; rebuild guidance lives in `dashboards/powerbi/BUILD_CHECKLIST.md`.
