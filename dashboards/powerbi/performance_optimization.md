# Performance Optimization

Use import mode, hide technical keys after relationships are created, avoid bi-directional filters, format measures rather than calculated columns where possible, and keep drill-through pages filtered by campaign/channel/date.
