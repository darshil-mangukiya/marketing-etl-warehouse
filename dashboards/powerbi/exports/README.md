# Optional Desktop Exports

The completed dashboard is committed at `dashboards/powerbi/p2_marketing_performance_dashboard.pbix`. Export a PDF from Power BI Desktop when a static review artifact is useful.
