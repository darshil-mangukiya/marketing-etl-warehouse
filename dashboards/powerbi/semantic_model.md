# Semantic Model

Use a dedicated Measures table. Measure definitions come from `dax_measures.dax` and reference `semantic/kpi_catalog.yml` keys in comments.

Semantic tables: 17. Relationship definitions: 11.
