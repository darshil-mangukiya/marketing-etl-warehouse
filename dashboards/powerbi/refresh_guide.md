# Refresh Guide

Regenerate the project exports with `make powerbi-exports`, then refresh the folder source in Power BI Desktop. For a published report, replace local file paths with a OneDrive, SharePoint, or workspace dataflow path.
